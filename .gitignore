# 凭证 / 本地配置(永不提交)
config.yaml
gateway.sh
.env
.env.*
*token*.txt
*.pem
*.key

# SSH host key(服务端私钥)
ssh_host_ed25519_key
ssh_host_ed25519_key.pub

# TLS 终结 CA(私钥 + 分发给设备的证书)
ccgw_ca_key
ccgw_ca_key.crt

# Go 构建产物
/claude-credential-gateway
*.exe
/dist/

# 部署时的回滚副本
*.old

# 测试 / 覆盖率
*.out
*.test
